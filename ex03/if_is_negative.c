#include <unistd.h>

void ft_is_negative (void) {
    int stmt = 1;
    if (stmt1) {
    // (n >= 0)
    } else {
    // (n < 0)
    }
    int positive_negative = (stmt1) ? 1 == P : 0 = N;
    return (0);
}
